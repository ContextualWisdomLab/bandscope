# Reduced-motion first tuning-plan navigation

Open tonight's first tuning plan with `behavior: "auto"` when `prefers-reduced-motion: reduce` matches. Do not keep a smooth scroll for that next action.
