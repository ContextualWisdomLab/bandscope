# Reduced-motion first dynamics-plan navigation

Open tonight's first dynamics plan with `behavior: "auto"` when `prefers-reduced-motion: reduce` matches. Do not keep a smooth scroll for that next action.
