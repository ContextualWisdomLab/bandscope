[package]
name = "bandscope-distribution-download"
version = "0.1.0"
edition = "2021"
description = "Bounded streaming updater-artifact admission for BandScope Distribution."
publish = false

[workspace]

[lints.rust]
unsafe_code = "forbid"
warnings = "deny"
missing_docs = "deny"
