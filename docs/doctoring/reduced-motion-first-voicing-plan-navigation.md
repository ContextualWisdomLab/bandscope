# Reduced-motion first voicing-plan navigation

Open tonight's first voicing plan with `behavior: "auto"` when `prefers-reduced-motion: reduce` matches. Do not keep a smooth scroll for that next action.
