# Reduced-motion first articulation-plan navigation

Open tonight's first articulation plan with `behavior: "auto"` when `prefers-reduced-motion: reduce` matches. Do not keep a smooth scroll for that next action.
