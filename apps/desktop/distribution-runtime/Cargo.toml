[package]
name = "bandscope-distribution-runtime"
version = "0.1.0"
edition = "2021"
description = "Bounded provisional updater-metadata admission for BandScope Distribution."
publish = false

[dependencies]
bandscope-distribution-core = { path = "../distribution-core" }

[workspace]

[lints.rust]
unsafe_code = "forbid"
warnings = "deny"
missing_docs = "deny"
