[package]
name = "bandscope-distribution-state"
version = "0.1.0"
edition = "2021"
description = "Crash-resilient local freshness state for BandScope Distribution/update decisions."
publish = false

[dependencies]
bandscope-distribution-core = { path = "../distribution-core" }

[workspace]

[lints.rust]
unsafe_code = "forbid"
warnings = "deny"
missing_docs = "deny"
