# Reduced-motion first pickup-plan navigation

Open tonight's first pickup plan with `behavior: "auto"` when `prefers-reduced-motion: reduce` matches. Do not keep a smooth scroll for that next action.
