# Reduced-motion first cutoff-plan navigation

Open tonight's first cutoff plan with `behavior: "auto"` when `prefers-reduced-motion: reduce` matches. Do not keep a smooth scroll for that next action.
